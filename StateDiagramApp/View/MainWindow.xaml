<Window x:Class="StateDiagramApp.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:bhv="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:StateDiagramApp.View"
        xmlns:vm="clr-namespace:StateDiagramApp.ViewModel"
        xmlns:converter="clr-namespace:StateDiagramApp.Converter"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate x:Key="RadioButtonTemplate1">
            <StackPanel>
                <Image Source="/Icons/Click.png"
               Width="32"
               Height="32"/>

            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="RadioButtonTemplate2">
            <StackPanel>
                <Image Source="/Icons/New.png"
               Width="32"
               Height="32"/>
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="RadioButtonTemplate3">
            <StackPanel>
                <Image Source="/Icons/Line.png"
               Width="32"
               Height="32"/>
            </StackPanel>
        </DataTemplate>

    </Window.Resources>
    <DockPanel >
        <Menu Height="20" DockPanel.Dock="Top">
            <MenuItem Header="_File(F)" InputGestureText="Ctrl+F" >
                <MenuItem Header="_Open(O)" InputGestureText="Ctrl+O" Command="{Binding FileOpen}"/>
                <MenuItem Header="_Save(S)" InputGestureText="Ctrl+S" Command="{Binding FileSave}"/>
                
                <Separator />
                <MenuItem Header="_Exit(X)" InputGestureText="Ctrl+X" />
            </MenuItem>
        </Menu>
        <ToolBar Height="50" DockPanel.Dock="Top">
            <RadioButton IsChecked="{Binding IsClickRadioButtonSelected}"
                         ContentTemplate="{StaticResource RadioButtonTemplate1}">
                
            </RadioButton>
            <RadioButton IsChecked="{Binding IsNewRadioButtonSelected}"
                         ContentTemplate="{StaticResource RadioButtonTemplate2}">
                
            </RadioButton>
            <RadioButton IsChecked="{Binding IsLineRadioButtonSelected}"
                         ContentTemplate="{StaticResource RadioButtonTemplate3}">
                
            </RadioButton>
        </ToolBar>
        <Canvas Background="White" >
            <bhv:Interaction.Triggers>
                <bhv:EventTrigger EventName="MouseDown">
                    <bhv:InvokeCommandAction Command="{Binding DataContext.MouseDownCommand2, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           CommandParameter="{Binding}" />
                </bhv:EventTrigger>

                <bhv:EventTrigger EventName="MouseUp">
                    <bhv:InvokeCommandAction Command="{Binding DataContext.MouseUpCommand2, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           CommandParameter="{Binding}" />
                </bhv:EventTrigger>

                <bhv:EventTrigger EventName="MouseMove">

                    <bhv:InvokeCommandAction Command="{Binding DataContext.MouseMoveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           CommandParameter="{Binding}" />
                </bhv:EventTrigger>
                
            </bhv:Interaction.Triggers>
            <ItemsControl ItemsSource="{Binding Shapes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Background="White"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding}">
                            <ContentControl.Resources>
                                <DataTemplate DataType="{x:Type vm:TransitionViewModel}">
                                    <local:TransitionControl >
                                        <bhv:Interaction.Triggers>
                                            <bhv:EventTrigger EventName="MouseDown">
                                                <bhv:InvokeCommandAction Command="{Binding DataContext.MouseDownCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           CommandParameter="{Binding}" />
                                            </bhv:EventTrigger>

                                            <bhv:EventTrigger EventName="MouseUp">
                                                <bhv:InvokeCommandAction Command="{Binding DataContext.MouseUpCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           CommandParameter="{Binding}" />
                                            </bhv:EventTrigger>
                                        </bhv:Interaction.Triggers>
                                    </local:TransitionControl>
                                </DataTemplate>
                                <DataTemplate DataType="{x:Type vm:NodeViewModel}">
                                    <local:NodeControl>
                                        <bhv:Interaction.Triggers>
                                            <bhv:EventTrigger EventName="MouseDown">
                                                <bhv:InvokeCommandAction Command="{Binding DataContext.MouseDownCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           CommandParameter="{Binding}" />
                                            </bhv:EventTrigger>

                                            <bhv:EventTrigger EventName="MouseUp">
                                                <bhv:InvokeCommandAction Command="{Binding DataContext.MouseUpCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           CommandParameter="{Binding}" />
                                            </bhv:EventTrigger>

                                            <bhv:EventTrigger EventName="MouseMove">

                                                <bhv:InvokeCommandAction Command="{Binding DataContext.MouseMoveCommand2, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           CommandParameter="{Binding}" />
                                            </bhv:EventTrigger>
                                        </bhv:Interaction.Triggers>
                                    </local:NodeControl>
                                </DataTemplate>
                            </ContentControl.Resources>
                        </ContentControl>
                    
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="Canvas.Left" Value="{Binding Left}" />
                        <Setter Property="Canvas.Top" Value="{Binding Top}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Canvas>
    </DockPanel>
</Window>

<Window x:Class="StateDiagramApp.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:bhv="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:StateDiagramApp.View"
        xmlns:vm="clr-namespace:StateDiagramApp.ViewModel"
        xmlns:converter="clr-namespace:StateDiagramApp.Converter"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Canvas x:Name="canvas" Background="White" >
        <bhv:Interaction.Triggers>
            <bhv:EventTrigger EventName="MouseMove">

                <bhv:InvokeCommandAction Command="{Binding DataContext.MouseMoveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                       CommandParameter="{Binding}" />
            </bhv:EventTrigger>
        </bhv:Interaction.Triggers>
        <ItemsControl ItemsSource="{Binding Shapes}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas Background="White"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding}">
                        <ContentControl.Resources>
                            <DataTemplate DataType="{x:Type vm:TransitionViewModel}">
                                <local:TransitionControl >
                                    <bhv:Interaction.Triggers>
                                        <bhv:EventTrigger EventName="MouseDown">
                                            <bhv:InvokeCommandAction Command="{Binding DataContext.MouseDownCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                       CommandParameter="{Binding}" />
                                        </bhv:EventTrigger>

                                        <bhv:EventTrigger EventName="MouseUp">
                                            <bhv:InvokeCommandAction Command="{Binding DataContext.MouseUpCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                       CommandParameter="{Binding}" />
                                        </bhv:EventTrigger>
                                    </bhv:Interaction.Triggers>
                                </local:TransitionControl>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type vm:NodeViewModel}">
                                <local:NodeControl >
                                    <bhv:Interaction.Triggers>
                                        <bhv:EventTrigger EventName="MouseDown">
                                            <bhv:InvokeCommandAction Command="{Binding DataContext.MouseDownCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                       CommandParameter="{Binding}" />
                                        </bhv:EventTrigger>

                                        <bhv:EventTrigger EventName="MouseUp">
                                            <bhv:InvokeCommandAction Command="{Binding DataContext.MouseUpCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                       CommandParameter="{Binding}" />
                                        </bhv:EventTrigger>
                                    </bhv:Interaction.Triggers>
                                </local:NodeControl>
                            </DataTemplate>
                        </ContentControl.Resources>
                    </ContentControl>
                    
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="Canvas.Left" Value="{Binding Left}" />
                    <Setter Property="Canvas.Top" Value="{Binding Top}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>
    </Canvas>
</Window>
